version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html
permissions:
  - object: /var/www/html
    pattern: "**"
    owner: apache
    group: apache
    mode: 755
hooks:
  AfterInstall:
    - location: scripts/restart_server.sh
      timeout: 60
      runas: root

